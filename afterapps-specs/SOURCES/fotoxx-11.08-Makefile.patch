--- Makefile	2011-07-30 15:43:40.000000000 -0500
+++ Makefile	2011-07-31 10:07:52.000000000 -0500
@@ -70,11 +70,6 @@
 	gzip -f -9 fotoxx.1
 	cp fotoxx.1.gz $(DESTDIR)$(MANDIR)
 	rm -f fotoxx.1.gz
-	# menu (desktop) file
-	echo Exec=$(BINDIR)/fotoxx >> $(DESTDIR)$(MENUFILE1)
-	echo Icon=$(SHAREDIR)/icons/fotoxx.png >> $(DESTDIR)$(MENUFILE1)
-	cp $(DESTDIR)$(MENUFILE1) $(DESTDIR)$(MENUFILE2)
-	@ echo "*** use make menu to install the desktop menu"
 
 menu:
 	xdg-desktop-menu install $(PREFIX)/share/applications/kornelix-fotoxx.desktop
